# Secrets: copy .env.example to .env and fill it in
.env

# Scrape state and deliverables (local CLI runs)
data/

# Web app job folders (local server runs)
jobs/

# Local tool binaries (Tailwind standalone, see TESTING.md)
bin/

.DS_Store
nohup.out
